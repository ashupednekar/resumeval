apiVersion: apps/v1
kind: StatefulSet 
metadata:
  name: resumeval
  labels:
    app: resumeval
spec:
  replicas: 2
  selector:
    matchLabels:
      app: resumeval
  template:
    metadata:
      labels:
        app: resumeval
    spec:
      containers:
      - name: resumeval
        image: ashupednekar535/resumeval:f89de29
        ports:
        - containerPort: 3001
          name: http
        env:
        - name: RUST_LOG
          value: "debug,sqlx=info,html5ever=info,scraper=info,standard_error=info,aws_smithy_runtime=info,lopdf=info"
        - name: LISTEN_PORT
          value: "3001"
        - name: SERVICE_NAME
          value: "resumeval"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: resumeval-secrets
              key: database-url
        - name: DATABASE_SCHEMA
          value: "resumeval"
        - name: SQLX_OFFLINE
          value: "true"
        - name: DATABASE_POOL_MAX_CONNECTIONS
          value: "5"
        - name: SMTP_USER
          value: "senderemailrs@gmail.com"
        - name: FROM_EMAIL
          value: "senderemailrs@gmail.com"
        - name: SMTP_PASS
          valueFrom:
            secretKeyRef:
              name: resumeval-secrets
              key: gmail-app-password
        - name: SMTP_SERVER
          value: "smtp.gmail.com"
        - name: SMTP_PORT
          value: "587"
        - name: BASE_URL
          value: "http://localhost:3000"
        - name: AI_PROVIDER
          value: "{{.Values.ai.provider}}"
        - name: AI_ENDPOINT
          value: ""
        - name: AI_MODEL
          value: ""
        - name: AI_KEY
          valueFrom:
            secretKeyRef:
              name: resumeval-secrets
              key: ai-key
        - name: S3_ENDPOINT
          value: "{{.Values.s3.endpoint}}"
        - name: S3_ACCESS_KEY
          value: "{{.Values.s3.access_key}}"
        - name: S3_SECRET_KEY
          value: "{{.Values.s3.secret_key}}" # TODO: take from secret
        - name: S3_BUCKET_NAME
          value: "files"
        - name: S3_REGION
          value: "us-east-1"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /livez
            port: 3001
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /healthz
            port: 3001
          initialDelaySeconds: 5
          periodSeconds: 30
        lifecycle:
          postStart:
            exec:
              command: 
              - /app/target/release/resumeval 
              - migrate
---
apiVersion: v1
kind: Service
metadata:
  name: resumeval
  labels:
    app: resumeval
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: resumeval
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: resumeval
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "{{.Values.ingress.issuer}}"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - "{{.Values.ingress.domain}}"
    secretName: resumeval-tls
  rules:
  - host: "{{.Values.ingress.domain}}"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: resumeval
            port:
              number: 80
